# Stock Muni

Control de stock con MERN (MongoDB + Express + React + Node).

## Requisitos
- Node.js (recomendado 18+)
- MongoDB (local o Atlas)

## Correr
1. Backend: `npm run dev:backend`
2. Frontend: `npm run dev:frontend`

Ver `backend/.env.example` para variables de entorno.

